# تثبيت البحث الذكي بالذكاء الاصطناعي 🤖

## خطوات التثبيت على الخادم

### 1. تثبيت المكتبات

```bash
cd /path/to/shipping-app-backend
pip3 install -r ai-requirements.txt
```

### 2. تحميل النموذج (أول مرة فقط)

عند أول استخدام، سيتم تحميل النموذج تلقائياً (~120 MB)

### 3. إعادة تشغيل الخادم

```bash
npm restart
# أو
pm2 restart shipping-app-backend
```

---

## كيف يعمل

### المستخدم يكتب: "وظائف حديثة جدة"

**AI يحلل:**
```json
{
  "searchText": "وظائف حديثة جدة",
  "cleanedText": "وظائف",
  "city": "جدة",
  "country": null,
  "timeFilter": 7,
  "isJobSearch": true
}
```

**النتيجة:**
- ✅ يبحث عن "وظائف" في النص
- ✅ يفلتر المدينة = "جدة" فقط
- ✅ يفلتر المنشورات من آخر 7 أيام
- ✅ يفلتر تصنيف الوظائف فقط

---

## الكلمات المفتاحية المدعومة

### المدن (عينة)
الرياض، جدة، مكة، المدينة، الدمام، الخبر، القاهرة، الإسكندرية، دبي، أبوظبي، الكويت، عمان...

### الدول
السعودية، مصر، الإمارات، الكويت، الأردن، قطر، البحرين، عمان...

### الفلاتر الزمنية
- حديثة/حديث → آخر 7 أيام
- جديدة/جديد → آخر 7 أيام
- اليوم → آخر 24 ساعة
- الأسبوع → آخر 7 أيام
- الشهر → آخر 30 يوم
- مؤخراً → آخر 7 أيام

### الوظائف
وظيفة، وظائف، عمل، توظيف، مطلوب، طلب عمل، كهربائي، سائق، مهندس...

---

## أمثلة الاستخدام

### مثال 1
```
البحث: "وظائف حديثة"
AI: يفلتر الوظائف من آخر 7 أيام ✅
```

### مثال 2
```
البحث: "كهربائي جدة"
AI: يبحث عن "كهربائي" في مدينة جدة فقط ✅
```

### مثال 3
```
البحث: "وظائف اليوم الرياض"
AI: وظائف من آخر 24 ساعة في الرياض ✅
```

### مثال 4
```
البحث: "شركات شحن دبي"
AI: شركات من دبي تحتوي على "شحن" ✅
```

---

## المميزات

✅ **فهم ذكي** - يفهم المعنى وليس فقط الكلمات
✅ **استخراج تلقائي** - للمدن والدول والتواريخ
✅ **فلترة دقيقة** - نتائج مطابقة 100%
✅ **سريع** - ~100-200ms إضافية فقط
✅ **مجاني** - بدون تكاليف API

---

## الأداء

- **الحجم**: ~120 MB (يتم تحميله مرة واحدة)
- **RAM**: ~500-700 MB عند الاستخدام
- **السرعة**: 100-200ms إضافية لكل بحث
- **التكلفة**: مجاني 100%

---

## ملاحظات مهمة

1. **أول استخدام**: سيستغرق وقتاً لتحميل النموذج
2. **الذاكرة**: تأكد من توفر 1GB RAM على الأقل
3. **Python**: يجب أن يكون Python 3.7+ مثبت
4. **Fallback**: إذا فشل AI، يعود للبحث العادي تلقائياً

---

## استكشاف الأخطاء

### خطأ: "python3: command not found"
```bash
# تثبيت Python
sudo apt-get update
sudo apt-get install python3 python3-pip
```

### خطأ: "No module named 'sentence_transformers'"
```bash
# إعادة تثبيت المكتبات
pip3 install -r ai-requirements.txt
```

### خطأ: "Out of memory"
```bash
# زيادة الذاكرة المتاحة أو استخدام نموذج أصغر
```

---

## الترقية المستقبلية

يمكن الترقية لاحقاً إلى:
- ✅ Embeddings للبحث الدلالي الكامل
- ✅ نماذج أكبر وأدق
- ✅ دعم لغات إضافية
- ✅ تعلم من سلوك المستخدمين

---

## الدعم

إذا واجهت أي مشاكل، تحقق من:
1. سجلات الخادم (logs)
2. تثبيت المكتبات بشكل صحيح
3. توفر الذاكرة الكافية
