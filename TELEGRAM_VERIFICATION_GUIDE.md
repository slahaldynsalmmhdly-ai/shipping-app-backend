# دليل إضافة ميزة التحقق من رقم الهاتف عبر بوت تيليجرام

## 📋 نظرة عامة

تم إضافة ميزة التحقق من رقم الهاتف باستخدام بوت تيليجرام إلى تطبيق الشحن. هذه الميزة تتيح للمستخدمين:

1. **التحقق من رقم الهاتف عند إنشاء حساب جديد**
2. **إعادة تعيين كلمة السر باستخدام رقم الهاتف**

تعمل الميزة بطريقة مشابهة لتطبيق واتساب، حيث يتلقى المستخدم رمز التحقق (OTP) عبر بوت تيليجرام بدلاً من الرسائل النصية القصيرة (SMS).

---

## 🤖 معلومات البوت

| المعلومة | القيمة |
|---------|--------|
| **اسم المستخدم** | `salah_ai_bot` |
| **رابط البوت** | [https://t.me/salah_ai_bot](https://t.me/salah_ai_bot) |
| **Token** | `8019318702:AAHf5S8MJCSWj2-rVtUl8B3IFXoaXAd61Ww` |

---

## 🔧 التغييرات في الخادم (Backend)

### 1. الملفات الجديدة المضافة

#### **`models/OTP.js`**
نموذج قاعدة البيانات لتخزين رموز التحقق (OTP). يحتوي على الحقول التالية:

- `phone`: رقم هاتف المستخدم
- `code`: رمز التحقق المكون من 6 أرقام
- `telegramChatId`: معرف محادثة تيليجرام (يتم تعبئته عندما يبدأ المستخدم محادثة مع البوت)
- `verified`: حالة التحقق (true/false)
- `expiresAt`: تاريخ انتهاء صلاحية الرمز (10 دقائق)
- `createdAt`: تاريخ الإنشاء

يستخدم هذا النموذج **TTL Index** في MongoDB لحذف الرموز تلقائيًا بعد انتهاء صلاحيتها.

#### **`services/telegramBot.js`**
خدمة إدارة بوت تيليجرام. تحتوي على الوظائف التالية:

- `initBot()`: تهيئة البوت وبدء الاستماع للرسائل
- `sendOTPViaTelegram(chatId, code)`: إرسال رمز التحقق مباشرة إلى محادثة محددة
- `getBotLink()`: الحصول على رابط البوت

**آلية العمل:**

عندما يرسل المستخدم أي رسالة إلى البوت، يقوم البوت بالبحث عن أحدث رمز OTP غير مرسل ويربطه بـ `chatId` الخاص بالمستخدم، ثم يرسل له الرمز تلقائيًا.

#### **`routes/phoneVerificationRoutes.js`**
مسارات API للتحقق من الهاتف. تحتوي على:

| المسار | الطريقة | الوصف |
|-------|---------|-------|
| `/api/v1/phone-verification/send-otp` | POST | إرسال رمز تحقق جديد |
| `/api/v1/phone-verification/verify-otp` | POST | التحقق من الرمز وإنشاء حساب |
| `/api/v1/phone-verification/forgot-password` | POST | إرسال رمز لإعادة تعيين كلمة السر |
| `/api/v1/phone-verification/reset-password` | POST | إعادة تعيين كلمة السر بعد التحقق |
| `/api/v1/phone-verification/otp-status/:otpId` | GET | التحقق من حالة إرسال الرمز |
| `/api/v1/phone-verification/resend-otp` | POST | إعادة إرسال رمز جديد |

### 2. التعديلات على الملفات الموجودة

#### **`models/User.js`**
تم إضافة الحقول التالية:

- `phoneVerified`: حالة التحقق من رقم الهاتف (Boolean)
- `telegramChatId`: معرف محادثة تيليجرام للمستخدم (String)
- تم تحديث `userType` لإضافة نوع جديد: `"recruitment_office"` (مكتب توظيف)

**القيم الممكنة لـ `userType`:**
- `individual` (فرد)
- `company` (شركة)
- `driver` (سائق)
- `recruitment_office` (مكتب توظيف) - **جديد**

#### **`server.js`**
تم إضافة:

1. استيراد مسارات التحقق من الهاتف:
   ```javascript
   const phoneVerificationRoutes = require("./routes/phoneVerificationRoutes");
   ```

2. تفعيل المسارات:
   ```javascript
   app.use("/api/v1/phone-verification", phoneVerificationRoutes);
   ```

3. تهيئة بوت تيليجرام عند بدء الخادم:
   ```javascript
   const { initBot } = require('./services/telegramBot');
   initBot();
   ```

#### **`.env.example`**
تم إضافة متغيرات البيئة التالية:

```env
# Telegram Bot Configuration (REQUIRED for phone verification)
TELEGRAM_BOT_TOKEN=8019318702:AAHf5S8MJCSWj2-rVtUl8B3IFXoaXAd61Ww
TELEGRAM_BOT_USERNAME=salah_ai_bot
```

---

## 📱 سير العمل (Workflow)

### السيناريو 1: إنشاء حساب جديد

يوضح المخطط التالي كيفية عمل الميزة عند إنشاء حساب جديد:

```
المستخدم يدخل بياناته (الاسم، تاريخ الميلاد، الجنس، الدولة، المدينة، رقم الهاتف)
    ↓
يضغط على "التالي"
    ↓
التطبيق يستدعي: POST /api/v1/phone-verification/send-otp
    ↓
الخادم ينشئ رمز OTP ويحفظه في قاعدة البيانات
    ↓
يظهر محمل (Loading) في التطبيق
    ↓
التطبيق يستدعي: GET /api/v1/phone-verification/otp-status/:otpId كل 3 ثوانٍ
    ↓
المستخدم يفتح تيليجرام ويرسل أي رسالة للبوت
    ↓
البوت يربط chatId بالـ OTP ويرسل الرمز للمستخدم
    ↓
الخادم يُحدث حالة OTP إلى "sent"
    ↓
التطبيق يكتشف أن الرمز تم إرساله (sent: true)
    ↓
يختفي المحمل وتظهر صفحة إدخال الرمز (6 خانات)
    ↓
المستخدم يدخل الرمز ويضغط "تحقق"
    ↓
التطبيق يستدعي: POST /api/v1/phone-verification/verify-otp
    ↓
الخادم يتحقق من الرمز وينشئ حساب المستخدم
    ↓
يتم تسجيل دخول المستخدم تلقائيًا
```

### السيناريو 2: إعادة تعيين كلمة السر

```
المستخدم يضغط على "نسيت كلمة السر"
    ↓
يدخل رقم هاتفه
    ↓
التطبيق يستدعي: POST /api/v1/phone-verification/forgot-password
    ↓
الخادم ينشئ رمز OTP ويحفظه
    ↓
نفس عملية الإرسال والتحقق كما في السيناريو 1
    ↓
المستخدم يدخل الرمز
    ↓
التطبيق يستدعي: POST /api/v1/phone-verification/reset-password
    ↓
الخادم يتحقق من الرمز ويسمح بتغيير كلمة السر
    ↓
المستخدم يدخل كلمة السر الجديدة
    ↓
يتم تحديث كلمة السر بنجاح
```

---

## 🚀 خطوات التشغيل

### 1. تثبيت المكتبات الجديدة

```bash
cd /home/ubuntu/shipping-app-backend
npm install node-telegram-bot-api
```

### 2. تحديث ملف `.env`

أضف المتغيرات التالية إلى ملف `.env`:

```env
TELEGRAM_BOT_TOKEN=8019318702:AAHf5S8MJCSWj2-rVtUl8B3IFXoaXAd61Ww
TELEGRAM_BOT_USERNAME=salah_ai_bot
```

### 3. تشغيل الخادم

```bash
npm start
```

يجب أن ترى الرسالة التالية في الـ console:

```
✅ Telegram Bot initialized successfully
```

### 4. اختبار البوت

1. افتح تيليجرام
2. ابحث عن `@salah_ai_bot`
3. أرسل `/start`
4. يجب أن يرد البوت برسالة ترحيبية

---

## 🧪 اختبار الـ APIs

### 1. إرسال رمز OTP

```bash
curl -X POST http://localhost:5000/api/v1/phone-verification/send-otp \
  -H "Content-Type: application/json" \
  -d '{"phone": "+966501234567"}'
```

**الاستجابة المتوقعة:**

```json
{
  "success": true,
  "message": "تم إنشاء رمز التحقق بنجاح",
  "botLink": "https://t.me/salah_ai_bot",
  "otpId": "507f1f77bcf86cd799439011"
}
```

### 2. التحقق من حالة الإرسال

```bash
curl http://localhost:5000/api/v1/phone-verification/otp-status/507f1f77bcf86cd799439011
```

**الاستجابة المتوقعة (قبل إرسال الرمز):**

```json
{
  "success": true,
  "sent": false,
  "expired": false,
  "verified": false
}
```

**الاستجابة المتوقعة (بعد إرسال الرمز):**

```json
{
  "success": true,
  "sent": true,
  "expired": false,
  "verified": false
}
```

### 3. التحقق من الرمز

```bash
curl -X POST http://localhost:5000/api/v1/phone-verification/verify-otp \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+966501234567",
    "code": "123456",
    "userData": {
      "name": "صلاح الدين",
      "email": "salah@example.com",
      "userType": "individual",
      "country": "السعودية",
      "city": "الرياض",
      "password": "password123"
    }
  }'
```

---

## 🎨 الواجهة الأمامية (Frontend)

### المطالبة للذكاء الاصطناعي

تم إعداد مطالبة كاملة للذكاء الاصطناعي لإنشاء صفحة التحقق من الرمز. الملف موجود في:

```
/home/ubuntu/ai_prompt.md
```

تحتوي المطالبة على:

- وصف كامل للصفحة والمكونات المطلوبة
- تفاصيل التصميم (6 حقول إدخال، زر فتح تيليجرام، عداد تنازلي)
- المنطق والوظائف (Polling، التحقق، إعادة الإرسال)
- الهيكل المقترح للملفات
- أمثلة على استدعاءات الـ API

---

## 📊 قاعدة البيانات

### نموذج OTP

```javascript
{
  phone: "+966501234567",
  code: "123456",
  telegramChatId: "123456789",  // يتم تعبئته عندما يبدأ المستخدم محادثة
  verified: false,
  expiresAt: "2025-11-16T12:30:00.000Z",  // 10 دقائق من وقت الإنشاء
  createdAt: "2025-11-16T12:20:00.000Z"
}
```

### تحديثات نموذج User

```javascript
{
  // ... الحقول الموجودة مسبقًا
  phone: "+966501234567",
  phoneVerified: true,  // جديد
  telegramChatId: "123456789",  // جديد
  userType: "recruitment_office"  // قيمة جديدة ممكنة
}
```

---

## 🔐 الأمان

1. **انتهاء صلاحية الرموز:** جميع رموز OTP تنتهي صلاحيتها تلقائيًا بعد 10 دقائق باستخدام TTL Index في MongoDB.

2. **استخدام لمرة واحدة:** بمجرد التحقق من الرمز، يتم تحديث حالته إلى `verified: true` ولا يمكن استخدامه مرة أخرى.

3. **حذف الرموز القديمة:** عند طلب رمز جديد لنفس رقم الهاتف، يتم حذف جميع الرموز القديمة.

4. **حماية التوكن:** يجب عدم مشاركة `TELEGRAM_BOT_TOKEN` مع أي شخص، ويجب تخزينه في متغيرات البيئة فقط.

---

## 🐛 استكشاف الأخطاء

### المشكلة: البوت لا يستجيب

**الحل:**
- تأكد من أن `TELEGRAM_BOT_TOKEN` صحيح في ملف `.env`
- تأكد من أن الخادم يعمل وأن البوت تم تهيئته بنجاح
- تحقق من الـ console للتأكد من وجود رسالة `✅ Telegram Bot initialized successfully`

### المشكلة: الرمز لا يصل للمستخدم

**الحل:**
- تأكد من أن المستخدم أرسل رسالة للبوت (أي رسالة، حتى `/start`)
- تحقق من أن `telegramChatId` تم تعبئته في قاعدة البيانات
- تحقق من الـ console للبحث عن أي أخطاء في إرسال الرسائل

### المشكلة: "رمز التحقق غير صحيح"

**الحل:**
- تأكد من أن الرمز لم تنتهِ صلاحيته (10 دقائق)
- تأكد من إدخال الرمز بشكل صحيح (6 أرقام)
- تحقق من أن رقم الهاتف المُرسل في طلب التحقق يطابق رقم الهاتف المُستخدم عند طلب الرمز

---

## 📝 ملاحظات إضافية

1. **عدم استخدام SMS:** هذا النظام لا يستخدم الرسائل النصية القصيرة (SMS)، بل يعتمد بالكامل على تيليجرام، مما يوفر التكاليف ويزيد من الأمان.

2. **تجربة المستخدم:** يجب على المستخدم أن يكون لديه حساب تيليجرام لاستخدام هذه الميزة.

3. **التوسع المستقبلي:** يمكن إضافة طرق تحقق بديلة (مثل البريد الإلكتروني) للمستخدمين الذين لا يستخدمون تيليجرام.

4. **الإشعارات:** يمكن استخدام نفس البوت لإرسال إشعارات أخرى للمستخدمين في المستقبل (مثل تحديثات الطلبات، رسائل النظام، إلخ).

---

## 📞 الدعم

إذا واجهت أي مشاكل أو كان لديك أسئلة، يرجى مراجعة:

- ملف `ai_prompt.md` للحصول على تفاصيل الواجهة الأمامية
- ملف `telegram_bot_instructions.md` لتعليمات إنشاء بوت تيليجرام
- الكود المصدري في مجلد `routes/` و `services/` و `models/`

---

**تم إنشاء هذا الدليل بواسطة Manus AI**
